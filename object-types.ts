const object: object = {};

object.valueOf();
object.toString();

const newUser: {
  name: string,
  lastNAme: string,
  height: number
} = {
  name: 'John',
  lastNAme: 'Doe',
  height: 180
};

// We can assign only number to the height property or only a string to the name and the lastName properties